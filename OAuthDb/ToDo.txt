TODO >
TokenJWT > int GetTimeForJwtTokenFromJan1970() > Hours is plus 2!!

UserRoleDAO > in Service bouwen

OAUthDBService >> Create Or Update new RoleActions

User can ONLY Have one ROLE >> Primary Key == only usr_id


/////

rdr.GetValue(0); // More in detail when everything is ready....